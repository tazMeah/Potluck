import Guest from "../models/Guest";
export default function GuestRow(item:Guest) {
    return (
    <tr className="GuestRow">
        <td>{item.name}</td>
        <td>{item.dish}</td>
    </tr>
    )
}